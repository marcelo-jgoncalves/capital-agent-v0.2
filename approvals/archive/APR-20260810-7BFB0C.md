# Critical Decision Approval Request

- Approval ID: APR-20260810-7BFB0C
- Date: 2026-08-10T11:49:11-03:00
- Proposed action: Enable Yahoo Finance MCP server (read-only market data)
- Category: data-adapter
- Capital required: BRL 0.00
- Maximum plausible loss: BRL 0.00
- Recurring commitment: False
- Status: PENDING

## Criticality reasons
- new read-only financial data adapter

## Why this action

First read-only financial data adapter for the Capital Agent, per ARCHITECTURE.md 'Financial data adapters'. Package: yahoo-finance-mcp-server (danishashko/yahoo-finance-mcp on GitHub), installed via npx, no API key required, backed by the yfinance Python library against Yahoo Finance's public endpoints. Reviewed via GitHub README (WebFetch, not full source audit): purely read-only (quotes, historical prices, company financials, analyst ratings, options chain data, news), explicitly documented for personal/educational/research use, no trading/order/account-modification capability advertised. Will be configured in .mcp.json for this project so it is available to future Claude Code sessions; does not touch data/ledger.csv or any execution/ path.

## Alternatives

- Doing nothing: continue with WebSearch/WebFetch only. Sufficient for qualitative research but not for structured price/fundamentals comparison across candidates.
- Alpha Vantage MCP: broader asset coverage (forex, crypto, commodities) and official/institutional backing, but requires the human to create a free API key first (Gate H2) before it can be used at all. Deferred, not rejected — worth adding later if a candidate needs forex/crypto data this adapter doesn't cover.
- Paid enterprise providers (Intrinio, EODHD, Financial Datasets, Twelve Data, Tiingo, Nasdaq Data Link): rejected for now, no budget justification against a BRL 1,000 experiment.

## Critic assessment

- Source scrutiny: reviewed via the repository's README content (WebFetch), not a full line-by-line source audit. The maintainer (`danishashko`) is an individual GitHub account, not an officially vetted publisher — residual supply-chain risk exists (arbitrary code execution via `npx` on each invocation) even though the *documented* capability is read-only.
- Mitigating factors: no credential/API key is created or stored (nothing for it to exfiltrate); it calls the public `yfinance`/Yahoo Finance endpoint, a widely used and inspected data path; it is scoped as a read-only MCP tool a Claude Code session can call, not a service with standing background access; the custody invariant means even a maximally malicious version of this server has no path to move money, since no execution capability exists anywhere in this repository for it to abuse (verified repeatedly by `tests/test_custody_and_execution.py`).
- Residual risk accepted: possible bad/incorrect data (mitigate by treating its output as `UNTRUSTED EXTERNAL CONTEXT` like any other web data, per `ARCHITECTURE.md`, never as authority); possible package-maintenance abandonment (low cost to remove if so).
- Recommendation: approve for read-only research use; do not treat its output as sufficient evidence alone for a material decision without cross-checking, and do not grant it any credential beyond what it already needs (none).

## Policy checks

- [x] No leverage, no borrowing, no withdrawal capability requested or granted.
- [x] No financial credential created (package requires none).
- [x] Read-only only — `config/policy.json`'s `financial_credentials_must_be_read_only` is satisfied trivially (no credential at all).
- [x] Does not touch `data/ledger.csv` or `execution/human_requests/` in any way.
- [x] Consistent with `ARCHITECTURE.md` "Financial data adapters" (read-only interface only) and `HUMAN_GATES.md` Gate H4 (first read-only adapter requires this approval).
- [x] Scope is limited to this one package/capability; does not authorize other adapters or a write upgrade path (none exists).

## Exact authorization requested

Authorize only the bounded action described above and only within the stated capital/loss limits.

## Human decision

APPROVED (interactive session, single-operator machine)

- Captured at: 2026-08-10T11:49:38-03:00
- Human statement (verbatim): "pode instalar o yahoo por enquanto"
